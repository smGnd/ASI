<h1>Listing books</h1>

<table>
  <tr>
    <th>Cover</th>
    <th>Author</th>
    <th>Title</th>
    <th>Isbn</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @books.each do |book| %>
  <tr>
    <td><a href="<%= book.cover %>"><img src="<%= book.cover %>" width="15" height="50"></a></td>
    <td><%= book.author %></td>
    <td><%= book.title %></td>
    <td><%= book.isbn %></td>
    <td><%= link_to 'Show', book %></td>
    <td><%= link_to 'Edit', edit_book_path(book) %></td>
    <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Book', new_book_path %>
